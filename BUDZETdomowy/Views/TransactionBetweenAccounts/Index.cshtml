@model IEnumerable<HomeBudget.Models.TransactionBetweenAccounts>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="text-center">
    <form method="post" asp-controller="TransactionBetweenAccounts" asp-action="ExportToCSV">
        <input type="submit" value="Export data to CSV"></input>
    </form>
</div>

<p></p>

<div class="text-center">
    <form method="post" asp-controller="TransactionBetweenAccounts" asp-action="ExportToExcel">
        <input type="submit" value="Export data to Excel"></input>
    </form>
</div>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SenderAccount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipientAccount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SenderAccount.Currency)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SenderAccount.AccountName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecipientAccount.AccountName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SenderAccount.Currency.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
